This is a very simple todo package for Emacs.

It requires Emacs 29 (or newer) with sqlite support. 

Usage:

  M-x anddo

Then `C-h b' or look in the menus for commands -- they're pretty self
explanatory.